---
// Astro コンポーネントのスクリプトはここ
---

<header class="cyberpunk-header">
    <div class="title-container">
        <h1 class="main-title glitch">System Admin</h1>
    </div>
    
    </header>

<style>
    /* ------------------------------ */
    /* ベーススタイル */
    /* ------------------------------ */
    .cyberpunk-header {
        position: relative;
        /* 背景を完全に透明化 */
        background-color: transparent; 
        color: #00ffc0; 
        padding: 20px 0; 
        min-height: auto; 
        overflow: hidden; 
        /* 境界線とシャドウも削除または透明化 */
        border-bottom: 2px solid transparent; 
        box-shadow: none;
    }

    /* ------------------------------ */
    /* タイトルコンテナ (中央配置とSF的な枠) */
    /* ------------------------------ */
    .title-container {
        position: relative;
        z-index: 10;
        max-width: fit-content;
        margin: 0 auto;
        padding: 5px 15px;
        
        /* 枠の色とシャドウは維持 (透過した背景の上に表示) */
        border: 1px solid rgba(0, 192, 255, 0.4);
        box-shadow: 0 0 10px rgba(0, 192, 255, 0.3), inset 0 0 5px rgba(0, 192, 255, 0.2);
    }

    .main-title {
        z-index: 10;
        font-size: 4rem;
        margin: 0;
        font-family: 'Orbitron', sans-serif; 
        text-transform: uppercase;
        letter-spacing: 5px;
        white-space: nowrap;
        
        /* ネオンエフェクトは維持 */
        text-shadow: 
            0 0 10px #00ffc0,
            0 0 20px #00ffc0,
            0 0 40px rgba(0, 255, 192, 0.7);
    }

    /* ------------------------------ */
    /* グリッチエフェクト (アニメーションは維持) */
    /* ------------------------------ */
    .glitch {
        animation: glitch 1.5s infinite linear alternate;
    }

    @keyframes glitch {
        0% {
            text-shadow: 
                0 0 10px #00ffc0,
                0 0 20px #00ffc0,
                -5px 0 0 #ff00ff, 
                5px 0 0 #00c0ff;
            transform: translate(0);
        }
        25% {
            text-shadow: 
                0 0 10px #00ffc0,
                0 0 20px #00ffc0,
                2px 2px 0 #ff00ff, 
                -2px -2px 0 #00c0ff;
            transform: translate(-5px, 5px);
        }
        50% {
            text-shadow: 
                0 0 10px #00ffc0,
                0 0 20px #00ffc0,
                0 0 0 #ff00ff, 
                0 0 0 #00c0ff;
            transform: translate(0);
        }
        75% {
            text-shadow: 
                0 0 10px #00ffc0,
                0 0 20px #00ffc0,
                -1px -3px 0 #ff00ff, 
                3px 1px 0 #00c0ff;
            transform: translate(3px, -3px);
        }
        100% {
            text-shadow: 
                0 0 10px #00ffc0,
                0 0 20px #00ffc0,
                5px 0 0 #ff00ff, 
                -5px 0 0 #00c0ff;
            transform: translate(0);
        }
    }
    
    /* ------------------------------ */
    /* レスポンシブ対応 (画面幅が小さい時) */
    /* ------------------------------ */
    @media (max-width: 768px) {
        .main-title {
            font-size: 2.5rem; 
            letter-spacing: 3px;
        }

        .cyberpunk-header {
            padding: 15px 0;
        }
        
        .title-container {
            padding: 5px 10px;
        }
    }
    
    @media (max-width: 480px) {
        .main-title {
            font-size: 1.8rem;
            letter-spacing: 2px;
        }
    }
</style>