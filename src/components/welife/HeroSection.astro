---
// src/components/welife/HeroSection.astro (例: ファイル名)
// 必要であれば Props を受け取ることも可能
---
<section class="relative w-full h-screen overflow-hidden flex items-center justify-center">

  <div class="absolute inset-0 z-0 bg-gradient-to-br from-gray-900 to-black">
    <div class="absolute inset-0 bg-[url('/images/welife/futuristic-texture.png')] opacity-20"></div>
    <div class="absolute inset-0 bg-[url('/images/welife/grid-pattern.png')] opacity-10 animate-pulse-subtle"></div>
  </div>

  <video src="/videos/welife/welife-hero.mp4" 
         autoplay loop muted playsinline disablePictureInPicture
         class="absolute top-0 left-0 w-full h-full object-cover opacity-50 z-10"></video>

  <div class="absolute inset-0 bg-gradient-to-b from-transparent to-black/80 z-20">
    <div class="absolute inset-0 bg-[url('/images/welife/grain-overlay.png')] opacity-20 pointer-events-none"></div>
  </div>

  <div class="relative z-30 flex flex-col items-center justify-center text-center px-4 max-w-5xl mx-auto">
    <h1 class="text-4xl sm:text-5xl md:text-7xl font-extrabold mb-8 neon-glow-h1 animate-fade-in-up animate-delay-300">
      <span class="block">パソコン教室</span>
      <span class="block text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-600">WeLife</span>
    </h1>

    <p class="text-lg sm:text-xl md:text-3xl neon-glow-p opacity-90 leading-relaxed max-w-4xl animate-fade-in-up animate-delay-600">
      <span class="block">あなたのオリジナルのシステムを</span>
      <span class="block">あなたのアートを表現するために</span>
    </p>

    <button class="mt-12 px-8 py-4 bg-gradient-to-r from-blue-500 to-purple-600 text-white text-xl font-bold rounded-full shadow-lg hover:from-blue-600 hover:to-purple-700 transition-all duration-300 transform hover:scale-105 neon-button-glow animate-fade-in animate-delay-900">
      無料体験を申し込む
    </button>
  </div>

</section>

<style>
  /* Google FontsのOrbitronをLayout.astroで読み込んでいる前提 */
  h1 {
    font-family: 'Orbitron', sans-serif; /* 未来的なフォントを適用 */
  }

  /* ネオンエフェクトとアニメーション */
  .neon-glow-h1 {
    color: #00eaff; /* 明るいサイバーブルー */
    text-shadow:
      0 0 10px rgba(0, 234, 255, 0.8),
      0 0 30px rgba(0, 234, 255, 0.6),
      0 0 60px rgba(0, 234, 255, 0.4);
  }

  .neon-glow-p {
    color: #b3f0ff; /* やや落ち着いたブルー */
    text-shadow:
      0 0 8px rgba(179, 240, 255, 0.6),
      0 0 15px rgba(179, 240, 255, 0.4);
  }

  .neon-button-glow {
    box-shadow: 0 0 15px rgba(100, 100, 255, 0.8);
    transition: box-shadow 0.3s ease-in-out;
  }
  .neon-button-glow:hover {
    box-shadow: 0 0 25px rgba(120, 120, 255, 1);
  }

  /* アニメーションキーフレーム */
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes pulse-subtle {
    0%, 100% { opacity: 0.1; }
    50% { opacity: 0.15; }
  }

  /* Tailwind CSSのUtilityクラスとしてアニメーションを定義 */
  .animate-fade-in-up {
    animation: fade-in-up 1s ease-out forwards;
    opacity: 0; /* 初期状態を非表示に */
  }
  .animate-pulse-subtle {
    animation: pulse-subtle 4s infinite ease-in-out;
  }
  .animate-delay-300 { animation-delay: 0.3s; }
  .animate-delay-600 { animation-delay: 0.6s; }
  .animate-delay-900 { animation-delay: 0.9s; }
</style>